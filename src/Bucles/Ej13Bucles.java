package Bucles;

public class Ej13Bucles {
    public static void main(double[] args) {
        boolean noessencer = false;
        double[] patrockNumbers = {1, 2, 3, 4, 3, -4, 5, 2, 5.4, 8};

        int comptador = 0;
        while (noessencer == false && comptador<patrockNumbers.length) {
            if (patrockNumbers[comptador] % 1 != 0) {
                noessencer=true;
            }

        else{




             }



        comptador++;

        }

    }
}
